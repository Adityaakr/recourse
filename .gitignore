# binaries / toolchain artifacts
bin/
target/
node_modules/
dist/

# secrets — never commit keys; .env.example is the documented surface
.env
.env.*
!.env.example

# data + runtime artifacts (delivered solutions, verifier evidence)
*.sqlite
*.sqlite-journal
.artifacts/
*.tsbuildinfo

# local
.DS_Store
.prism/runs/
vara-eth-skills/
